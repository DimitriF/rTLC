rTLC V.1.0
===========

This shiny application is made to perform multivariate analysis of HPTLC chromatograms.

## Web application

Available on our server hosted in the Justus Liebig University of Giessen, Germany:

http://134.176.7.66/rTLC/

## Installation

Install R
https://www.r-project.org/

#### From cran

Incomming (may be)

#### From github

In the R console, install the devtools package with those commands
```r
install.packages("devtools")
library(devtools)
install_github("dimitrif/rTLC")
```

Then, run this command to launch the application
```r
rTLC::run.rTLC()
```
